<!DOCTYPE html>
<html lang="de-DE">

  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script src="https://kit.fontawesome.com/6ea270afe6.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/style/style.css">
    <link rel="stylesheet" href="/style/vex.css">
    <title>{{vex.name}} - Verhaltensexperiment</title>
  </head>

  <body>
    {{> navbar}}

    <main class="content vex-pre">
      {{#with vex}}
      <section class="vex-header">
        <h2 class="vex-date">{{dateConversion date}}</h2>
        <h1 class="vex-headline">{{name}}</h1>
      </section>
      <section class="vex-body">
        <form action="/v/new/{{_id}}/pre" method="post" class="vex-form vex-pre-form">
          <div class="vex-form-field">
            <label for="situation">Situation*</label>
            <textarea type="textbox" name="situation" class="vex-input vex-textbox" required>{{situation}}</textarea>
          </div>
          <div class="vex-form-field">
            <label for="personen">Personen</label>
            <textarea type="textbox" name="personen" class="vex-input">{{personen}}</textarea>
          </div>
          <div class="vex-form-field">
            <label for="sicherheitsverhalten">Sicherheitsverhalten</label>
            <textarea type="textbox" name="sicherheitsverhalten" class="vex-input">{{sicherheitsverhalten}}</textarea>
          </div>
          <div class="vex-form-field">
            <label for="befürchtungen">Befürchtungen</label>
            <textarea type="textbox" name="befürchtungen" class="vex-input">{{befürchtungen}}</textarea>
          </div>
          <div class="vex-form-field">
            <label for="experiment">Experiment</label>
            <textarea type="textbox" name="experiment" class="vex-input">{{experiment}}</textarea>
          </div>
          <div class="vex-form-field btn-bottom-right">
            <button type="submit">
              <i class="fa-solid fa-arrow-right"></i>
            </button>
          </div>
        </form>
      </section>
      {{/with}}
    </main>

    {{> footer}}
  </body>

  <script src="/style/style.js"></script>

</html>